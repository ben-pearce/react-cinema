import React from 'react';
import ReactDOM from 'react-dom';
import Cinema from './components/CinemaApp.jsx';

import './css/styles.scss';

const wrapper = document.getElementById('cinema');
ReactDOM.render(<Cinema />, wrapper);
